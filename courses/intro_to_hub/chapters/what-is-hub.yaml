- Category: code
  Prompt: "Check if import is succesful. Type `hub.Dataset`"
  Answer: hub.Dataset

- Category: code
  Prompt: "Let's load a Dataset. Type `ds = hub.Dataset('hub://activeloop/mnist-train')`"
  Answer: ds = hub.Dataset("hub://activeloop/mnist-train")
  Hints: "Check the syntax."

- Category: code
  Prompt: "Time to check the metadata. Just type `print(ds)`"
  Answer: print(ds)
